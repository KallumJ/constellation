import 'package:flutter/material.dart';

class NewImagePostScreen extends StatelessWidget {
  const NewImagePostScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("Image Post Screen"),
      ),
    );
  }
}